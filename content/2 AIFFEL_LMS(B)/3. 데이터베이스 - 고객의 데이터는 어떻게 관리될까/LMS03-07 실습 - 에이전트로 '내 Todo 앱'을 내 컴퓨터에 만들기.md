# [3.데이터베이스 - 고객의 데이터는 어떻게 관리될까] - 7. 실습 - 에이전트로 '내 Todo 앱'을 내 컴퓨터에 만들기

---

## 실습: 에이전트로 '내 Todo 앱'을 내 컴퓨터에 만들기 (따라 하기)

이제 배운 걸 모아 **직접 만들어 봅니다.** 만들 것은 **내 컴퓨터에서 도는 작은 Todo(할 일) 앱** - 할 일을 추가·확인·완료·삭제하고, 그 데이터가 **내 컴퓨터의 무료 DB** 에 저장돼 껐다 켜도 남습니다. Todo 앱은 **저장·조회·수정·삭제(CRUD)** 의 교과서라 DB를 손에 익히기에 딱입니다.

방식은 **에이전틱** - 목표(PRD)를 주면 에이전트가 계획·설치·구현을 하고, 우리는 검토·판단·수정합니다.

**두 가지 길을 다 해 봅니다**(원하는 쪽부터).

- **길 A - 내 컴퓨터(로컬)**: 무료 로컬 DB(예: SQLite)에 저장. 인터넷 없이 내 PC에서 완결됩니다.
- **길 B - 클라우드(Supabase)**: 같은 앱을 클라우드 DB로. 남에게 링크로 보여줄 때.

**도구** - Claude Code나 Cursor의 에이전트 모드처럼 **명령 실행·파일 생성·설치를 스스로 하는** 도구를 씁니다. AI가 짠 코드는 한 번씩 훑어보세요.

### 1부: PRD로 시작하고, DB 구조를 설계받기

앞서 배운 대로 **PRD(만들 것 한 장)** 부터 씁니다. Todo 앱의 **기본 기능 + 내가 넣고 싶은 기능** 을 적으세요.

```
[무엇] 내 컴퓨터에서 도는 Todo(할 일) 앱.
[기본 기능(CRUD)] 할 일 추가 / 목록 보기 / 완료 표시 / 삭제
[저장] 데이터는 내 컴퓨터의 무료 DB에 저장(껐다 켜도 남기)
[내가 넣고 싶은 기능] <- 여기에 1~2개 직접 적기
  예: 마감일 표시 / 카테고리(태그) / 오늘 할 일만 보기 / 검색
[규칙] 무료 도구만. 비밀값은 .env로.
(로그인 같은 걸 넣는다면 직접 만들지 말고 검증된 라이브러리로.)
```

이 PRD를 에이전트에게 주고, **먼저 DB 구조부터 설계받으세요.**

> 프롬프트: "위 PRD로 만들 Todo 앱의 **DB 구조(어떤 표·열·관계)** 를 먼저 설계해서 구조도로 그려 보여 줘. 내 커스텀 기능(마감일·태그 등)도 반영해서. 로컬 무료 DB(예: SQLite)를 쓸 거야."

→ **확인:** AI가 표(예: `todos`, 태그를 넣었다면 `tags`와 연결표)와 열(제목·완료여부·마감일…)을 구조도로 보여 줍니다. 앞서 배운 '표 사이 관계'대로 잡혔는지 눈으로 확인하세요.

예시 - 마감일·태그(카테고리)를 넣은 Todo 앱의 구조도. 할 일(todos)과 태그(tags)는 '연결표(todo_tags)'로 이어집니다(다대다). 이런 그림을 AI에게 받아 확인하면 됩니다.

![[day08-todo-erd.png]]
*출처: 모두의연구소 AIFFEL_LMS 강의자료. 마감일·태그를 넣은 Todo 앱의 DB 구조도 예시: todos와 tags가 연결표 todo_tags로 이어진다.*

### 2부: 길 A - 내 컴퓨터에 만들어 돌리기 (로컬)

DB 구조에 합의했으면, 에이전트에게 로컬 무료 DB로 실제 구현을 맡깁니다.

> 작업 지시서: "이 PRD와 방금 합의한 DB 구조로 Todo 앱을 만들어 줘. 데이터는 로컬 SQLite 파일 하나(`todo.db`)에 저장하고, 브라우저에서 `localhost`로 열리게 해 줘. `.env`·`todo.db`는 `.gitignore`에 넣어. 먼저 전체 계획을 보여주고, 각 단계 끝에 뭘 확인하면 되는지 알려 줘."

에이전트가 계획→설치→파일 생성→DB 만들기까지 스스로 합니다. 우리가 할 일은 셋.

**계획 한 번 읽기**(PRD와 맞는지) / **위험한 명령은 승인**(이 프로젝트 폴더 안인지 확인) / **잘 되던 순간마다 Git 커밋**(되돌릴 지점).

→ **통과:** 브라우저에서 할 일을 추가하면 목록에 뜨고, **새로고침·앱 재시작 후에도 남는다.** DB 뷰어로 `todo.db`를 열어 `todos` 표에 행이 실제로 쌓였는지 확인(확인 툴은 AI에게 추천받기 - SQLite는 'DB Browser for SQLite'나 VS Code의 SQLite 확장).

실제로 만들면 이런 화면이 뜹니다.

예시 - 실제로 만들어 돌린 Todo 앱 화면. 추가한 할 일이 목록에 남고, 데이터는 내 컴퓨터의 `todo.db`에 저장됩니다.

![[day08-todo-app-screenshot.png]]
*출처: 모두의연구소 AIFFEL_LMS 강의자료. 실제로 만들어 돌린 Todo 앱 화면: 제목 My Todo, 입력창과 추가 버튼, 마감일이 붙은 할 일 목록.*

**자주 막히는 곳**

| 증상 | 확인 / 해결 |
| :--- | :--- |
| `npm install`에서 `better-sqlite3` 빌드 에러(node-gyp) | 네이티브 모듈이라 빌드 도구가 필요. 에러를 그대로 에이전트에 주고 "내 OS(맥/윈도우) 기준 빌드 도구를 설치·확인해 줘"(맥: Xcode Command Line Tools, 윈도우: 빌드 툴·Python). Node는 LTS 권장 |
| 포트가 `already in use`(EADDRINUSE) | 이전 서버가 아직 떠 있음. 그 창을 끄거나 다른 포트로 |
| 추가했는데 새로고침하면 사라짐 | 화면에만 있고 DB엔 안 들어감. "추가한 할 일을 DB에 저장하고 다시 읽어오게 해 줘" |
| 코드 고쳤는데 반영 안 됨 | 개발 서버를 껐다 켜기(특히 `.env` 수정 후) |
| `todo.db`를 GitHub에 올려버림 | `.gitignore`에 추가. 이미 커밋했다면 에이전트에게 'git 이력에서도 빼 줘'까지 |

**안 되면** - 에러를 그대로 에이전트에게 붙여넣어 고치게 하세요(관찰→수정 반복). 같은 걸 계속 못 고치면 마지막으로 되던 지점으로 되돌리고 문제를 잘게 쪼개 다시 시키세요.

### 3부: 길 B - 클라우드(Supabase)로도, 그리고 마무리

같은 Todo 앱을 **클라우드 DB(Supabase)** 로도 만들어 두면, 나중에 **남에게 링크로** 보여줄 수 있습니다(로컬은 내 PC에서만 열리니까요).

> 프롬프트: "같은 Todo 앱을, 로컬 SQLite 대신 **Supabase(무료 클라우드 Postgres)** 에 저장하도록 바꿔 줘. 접속 키는 `.env`로 빼고, **RLS(행 수준 보안)를 켜서** 허용된 접근만 되게 해 줘."

→ **확인:** Supabase 대시보드의 **Table Editor** 에서 `todos` 표에 행이 쌓이는지 눈으로 확인. 그리고 앞서 배운 대로 **RLS 꼭 켜기** - 안 켜면 공개 키로 누구나 데이터에 접근할 수 있습니다.

**로컬 vs 클라우드, 언제 뭘?** - 개발·학습·오프라인은 **로컬**, 남에게 보여주고 24시간 열려면 **클라우드**. 흔한 흐름은 **'로컬에서 만들고 → 낼 때 클라우드로'** 입니다. 로컬로 익힌 뒤 배포할 땐 연결만 클라우드로 바꾸면 됩니다.

---

## 전체 커리큘럼 구성표

|  강좌 번호   | 강좌명                                     |
| :------: | :--------------------------------------- |
|  **1강**  | [[LMS03-01 데이터를 저장할 곳이 필요한 이유]]         |
|  **2강**  | [[LMS03-02 왜 종류가 많을까 - 데이터베이스의 역사]]     |
|  **3강**  | [[LMS03-03 SQL 구경과 데이터의 모양]]            |
|  **4강**  | [[LMS03-04 회원가입과 로그인 - 사용자를 다루기]]       |
|  **5강**  | [[LMS03-05 데이터를 안전하게 - 보안과 백업]]         |
|  **6강**  | [[LMS03-06 무료로 쓸 수 있는 DB 고르기]]          |
|  **7강**  | **7강. 실습 - 에이전트로 '내 Todo 앱'을 내 컴퓨터에 만들기 (현재)** |
|  **8강**  | [[LMS03-08 마무리 - 회고와 핵심 정리]]            |
